<body class="register-page">
  <div class="register-box">
    <div class="register-logo">
      <b>忘记密码</b>
    </div>

    <div class="register-box-body">
      <p class="login-box-msg">输入邮箱找回密码</p>
      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>
        <div class="hide form-group has-feedback">
          <input type="text" class="form-control" placeholder="Full name"/>
          <span class="glyphicon glyphicon-user form-control-feedback"></span>
        </div>
        <div class="form-group has-feedback">
          <%= f.email_field :email, required: true, autofocus: true, class: "form-control", placeholder: "邮箱" %>
          <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
        </div>

       <div class="row">
          <div class="col-xs-8">
            <div class="checkbox">
            </div>
          </div><!-- /.col -->
          <div class="col-xs-4">
            <%= f.submit "重置密码", class: "btn btn-primary btn-block btn-flat" %>
          </div><!-- /.col -->
        </div>
      <% end %>

      <div class="hide social-auth-links text-center">
        <p>- OR -</p>
        <a href="#" class="btn btn-block btn-social btn-facebook btn-flat"><i class="fa fa-facebook"></i> Sign up using Facebook</a>
        <a href="#" class="btn btn-block btn-social btn-google-plus btn-flat"><i class="fa fa-google-plus"></i> Sign up using Google+</a>
      </div>

      <%= link_to "登录", new_session_path(resource_name), class: 'text-center' %><br />
      <%= link_to "注册", new_registration_path(resource_name) %><br />
    </div><!-- /.form-box -->
  </div><!-- /.register-box -->
</body>
